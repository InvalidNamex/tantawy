from django.urls import path
from . import views

# URLConf
app_name = 'core'

urlpatterns = [
    # path('categories/', views.category_list, name='category_list'),
    # path('sub-categories/<int:id>/', views.subCategory_list, name='subcategory_list'),
]
