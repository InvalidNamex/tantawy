# 🚀 QUICK START GUIDE - إدارة المخزون

## ⚡ 3 خطوات سريعة للبدء

### الخطوة 1: إضافة مستخدم لمجموعة StoreAdmins

```bash
python manage.py shell
```

```python
from django.contrib.auth.models import User, Group

# استبدل 'admin' باسم المستخدم الخاص بك
user = User.objects.get(username='admin')
group = Group.objects.get(name='StoreAdmins')
user.groups.add(group)

print(f"✅ تم إضافة {user.username} لمجموعة StoreAdmins")
```

### الخطوة 2: التحقق من وجود مورد

```python
from core.models import CustomerVendor

vendors = CustomerVendor.objects.filter(type__in=[2,3], isDeleted=False)
print(f"عدد الموردين: {vendors.count()}")

# إذا كان العدد 0، قم بإنشاء مورد:
if vendors.count() == 0:
    CustomerVendor.objects.create(
        customerVendorName="مورد افتراضي",
        type=2,
        notes="للمخزون"
    )
    print("✅ تم إنشاء مورد افتراضي")
```

### الخطوة 3: استخدم النظام!

1. افتح المتصفح
2. سجل الدخول
3. انقر على "إدارة المخزون" في القائمة العلوية
4. اختر متجر
5. أضف أو اخصم الكميات!

---

## 📋 الروابط السريعة

- **قائمة المتاجر:** `/inventory/`
- **تفاصيل متجر:** `/inventory/store/<رقم_المتجر>/`

---

## 🎯 الميزات الرئيسية

✅ عرض جميع المتاجر  
✅ عرض الأصناف والكميات  
✅ إضافة كميات (فاتورة شراء)  
✅ خصم كميات (مرتجع شراء)  
✅ بحث في الأصناف  
✅ حماية بالصلاحيات  

---

## 🔐 الصلاحيات المطلوبة

- مجموعة: **StoreAdmins**
- أو: **Superuser**

---

## ❓ الأسئلة الشائعة

**س: لا أرى رابط "إدارة المخزون"؟**  
ج: تأكد من إضافتك لمجموعة StoreAdmins

**س: رسالة "لا يوجد موردين"؟**  
ج: قم بإنشاء مورد واحد على الأقل

**س: هل يؤثر على الفواتير الحقيقية؟**  
ج: نعم، يتم إنشاء فواتير بسعر 0 للحفاظ على التسلسل

---

## 📞 المساعدة

راجع: `INVENTORY_MANAGEMENT.md` للتوثيق الكامل

---

✨ **جاهز للاستخدام الآن!**
